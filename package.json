{
  "name": "arsenal",
  "type": "module",
  "module": "main.ts",
  "scripts": {
    "arsenal": "bun run --bun generate_arsenal.ts",
    "loadouts": "bun run --bun generate_loadouts.ts",
    "extract": "bun run --bun extract_config.ts",
    "check": "oxlint --type-aware --type-check && oxfmt --check",
    "lint": "oxlint --type-aware --type-check --fix",
    "format": "oxfmt --write"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "oxfmt": "^0.28.0",
    "oxlint": "^1.43.0",
    "oxlint-tsgolint": "^0.11.4"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}